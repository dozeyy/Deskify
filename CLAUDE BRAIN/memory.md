# AI Brain — Memory Log (Root)

## 2026-07-05
- Discord (single-instance Electron) still opened at "whatever the newest saved location is, no matter which deskspace." **The per-deskspace data was already correct** — `coding.json` and `music-production.json` each store their own Discord `AppEntry.Window` (monitor 0 @92,111 vs monitor 1 @1280,0); there is no shared/global one-position-per-app. The symptom was purely the launch **settle race** in `LaunchEngine.PositionWindowsAsync`: the settle loop did `if (allGood) break;` — it stopped re-correcting the instant the window matched **once**. Discord opens at its own internally-remembered bounds, briefly matches right after Deskify re-applies, then re-asserts that remembered position a beat later — landing back at the last-used ("newest") spot after we'd already stopped watching. Fix: require the layout to hold across **consecutive** passes (`requiredStable = 2`, tracked via `stableStreak`) before breaking, and treat a momentarily-missing match (mid-swap) as `allGood = false` so it keeps watching instead of possibly breaking on a gap. Also bumped settle attempts (non-strict 5→6, strict 8→10) to give the confirming pass headroom. General fix for any self-restoring single-instance app, not just Discord.
  - Verification follow-up (Will asked to confirm per-deskspace positioning works "every time"): traced full save→launch→settle path against the two real projects, confirmed build clean + app starts with no exception (the 13:13 `FileNotFoundException` was from the in-progress sound-removal work, gone in current build), confirmed switch closes+relaunches Discord fresh (`neverCloseApps` empty, `confirmCloseOthers:false`). Two reliability fixes applied with Will's OK:
    - **Strict is now the default for new projects**: `AppSettings.StrictLayoutDefault` default flipped `false`→`true` (and Will's on-disk `settings.json` updated). New deskspaces were getting the shorter non-strict settle window (6×700ms) while his existing Coding/Music projects were manually Strict (10×700ms) — Discord needs the longer window to reliably win back its self-restored position. Users can still turn Strict off per-project.
    - **Normalized `music-production.json`'s version-pinned Discord path** `...\app-1.0.9244\Discord.exe` → `Update.exe` + `args:"--processStart Discord.exe"` (matches `coding.json`), so it survives Discord updates instead of 404ing. `TargetExeName` already parses `--processStart` back to `discord.exe` for window matching.
- **Root-caused two "still broken" reports (Discord + browser) with real evidence, not theory:**
  - **Discord "still" wrong = Will was running a STALE build.** The running process (PID 7404, started 15:18) was `...\bin\Release\net8.0-windows\Deskify.exe` built **14:45** — *before* the day's fixes landed in source (~15:05–15:22). The settle-race fix (and everything else) was only in the newer Debug build; the log showed zero launches after the 15:07 verification run. Lesson: **when a fix "doesn't work," first confirm which binary is actually running** (`Get-Process Deskify | Select Path,StartTime` vs exe `LastWriteTime`) — a Release build locked/in-use is itself the tell. Rebuilt Release+Debug fresh (15:22/15:23) after stopping the stale instance.
  - **Browser landing wrong = `DefaultBrowser.Detect()` returned null on Will's machine.** His default browser is **Zen Browser** (a Firefox fork) at `C:\Program Files\Zen Browser\zen.exe`. Windows' `http\UserChoice` ProgId is a **stale hashed ProgId** `FirefoxURL-308046B0AF4A39CB` whose `shell\open\command` no longer exists (leftover from a prior Firefox/Zen version); the *working* registration is a sibling hash `FirefoxURL-F0DC299D809B9700`. Old `Detect()` did a single strict `HKCR\{progId}\shell\open\command` lookup → null → `SyncAutoLinkedEntries` never created a browser `AppEntry` → browser had no saved layout → it opened at its own remembered spot (and `OpenUrls` fell back to one-shell-open-per-URL). Fix: `Detect()` now falls back when the exact ProgId command is missing — (1) match by ProgId base name before the last `-` (enumerate `Registry.ClassesRoot` subkeys starting with e.g. `FirefoxURL-`), then (2) `StartMenuInternet` clients (skipping the IE stub). Verified by replaying the exact logic against the live registry: primary → empty, prefix fallback → `zen.exe`. **Caveat still to tell Will:** even with Detect() fixed, the browser entry is created *without* a Window; each project needs Edit Layout → arrange Zen → Save Layout once to capture the browser position for the first time (the old projects have no browser entry at all yet).
  - **Browser entry now re-syncs to the current default browser on every open (Will's request "always check default browser").** `SyncAutoLinkedEntries` already ran on load/launch/edit (incl. `ProjectStore.LoadAll`), but its browser block did delete-and-re-add on any path mismatch, which **discarded the saved window position** when the default browser changed. Rewrote it to update the single auto-linked browser entry **in place** — re-detect each time, set `Path`/`Name` to the current default, and **preserve `Window`** (inheriting from a stale sibling if the kept entry had none). If URLs exist but detection returns null, it now leaves the existing entry alone instead of nuking it over a transient miss. Net effect: switch Chrome→Zen and the browser still opens at the same saved spot; the entry always points at the real current default.
- **Reworked launch to "load fully, THEN snap," behind a please-wait overlay** (Will's request). Old `LaunchEngine.PositionWindowsAsync` positioned each window the instant it appeared (incremental), which fought apps still running their own startup layout and made windows visibly jump. New flow is phased: (1) wait until every target app has a window on screen or timeout — *without moving anything*; (2) brief pause (non-strict 1500ms / strict 2500ms) so apps finish their own positioning; (3) snap them all to saved positions in one pass; (4) the existing consecutive-stable settle loop holds them. New `LoadingOverlayWindow` (UI/LoadingOverlayWindow.xaml[.cs]) is a full-virtual-screen translucent scrim (`#B4000000`, covers all monitors via `SystemParameters.VirtualScreen*`) with a spinner + phase text ("Waiting for apps to finish loading… (n/total)" → "Letting things settle…" → "Arranging your windows…" → "All set — good to go!"), fades in/out. `MainWindow.LaunchProject` shows it **after** the close-confirm dialog (so it never hides it) and closes it in `finally` only when `IsCurrent()` (newest-wins: a superseded launch leaves the overlay to the newer one). Overlay is fed by the same `Progress<string>` that updates `StatusText`. Edit Layout is unchanged (user needs to see/arrange windows there). Builds clean (Debug+Release), starts clean.
- Found and fixed the real reason uninstall never worked: `installer/Deskify.Setup/App.xaml`'s `<Application>` root was missing `x:Class="Deskify.Setup.App"`. Without it, the XAML compiler silently generates a throwaway `XamlGeneratedNamespace.GeneratedApplication` as the actual entry point instead of wiring up `App.xaml.cs` — so the `/uninstall` command-line handling (and everything else in `OnStartup`) has never executed, since the installer was first created. An earlier upstream PR (7c18761) fixed the registry `UninstallString` arg and added filename-based uninstall detection but didn't catch this — that fix was dead code without the `x:Class` fix. Also fixed a real race in `Uninstaller.cs`'s self-delete (fixed 2s timer could fire before the user dismissed the "uninstalled" message box, leaving `Uninstall.exe` locked/undeleted) by polling for process exit via PowerShell instead. Verified live end-to-end: install, uninstall via the exact Add/Remove Programs path, and reinstall.
- Bumped the installer options-page window height 560→640 (content was taller than the fixed window, cutting off/overflowing past the Install button).
- Local repo had fallen behind `origin/main` by two merged PRs (cloud Claude sessions had already shipped a UI polish pass — sounds, transitions, button animations — plus a native macOS SwiftUI port) without a corresponding memory entry here. Rebased local fixes onto that instead of clobbering it. **Lesson: check `git fetch && git log main..origin/main` before assuming local HEAD is current — GitHub PRs can land from other sessions this repo's memory doesn't know about.**
- `Deskify-Setup.exe` (129MB+) exceeds GitHub's 100MB push limit — added `/Deskify-Setup.exe` to `.gitignore`. It was never actually committed before either (just sat untracked at repo root); distribute built installers via GitHub Releases (see the existing v0.1.0 release), not git.
- Fixed two reported bugs (FL Studio/browser windows "loading out of place", Explorer windows not closing on workspace switch):
  - `WindowCloser.OtherWindows` built its "keep" set purely from exe name — every File Explorer window shares `explorer.exe`, so the moment a project had ANY folder, EVERY open Explorer window (any folder, any project) silently counted as "belonging" and was never offered for closing on switch. Now Explorer windows are kept only if they're actually showing one of the project's folders (`ExplorerWindows.ShowsFolder`, tab-aware), same signal already used for window matching.
  - `LaunchEngine.PositionWindowsAsync`'s settle-and-reapply pass (catches apps like FL Studio or a browser restoring its own remembered size/position shortly after Deskify positions it) only ran when a project's Strict Layout toggle was on — off by default for new projects. It now always runs once (Strict mode still gets 3 passes instead of 1), so the exact "app moves itself right after opening" case it was built for is no longer opt-in.
- Follow-up (same day): browser still landed in the *previous* session's spot on switch, and Explorer occasionally landed wrong / threw a false "explorer.exe: left running (protected — never force-closed)". Root cause of both: a workspace switch only closed *unrelated* windows and kept the project's own single-instance apps (browser, File Explorer) alive — a kept-alive browser just gets new tabs and stays wherever it was last dragged, ignoring the saved position; repositioning a live single-instance window is unreliable because it re-asserts its own remembered bounds. Fix = make a switch a **clean slate**: `WindowCloser.AllWindows()` + `MainWindow.CloseAllForSwitchAsync` now close *everything* closeable (Deskify + anti-cheat still excluded by the scanner) before `LaunchAsync` reopens the project from scratch, so every window opens fresh at its saved layout. The confirm dialog (WorkspaceSwitch mode) still lets the user uncheck anything to keep; kept-alive project apps are still repositioned by the launch step.
  - Also fixed the false Explorer error at its source in `WindowCloser.CloseAndVerifyAsync`: it grouped windows by PID and waited for process exit, but every File Explorer window shares the one shell explorer.exe process that never exits — so a closed Explorer window always fell through to the "protected — never force-closed" branch. Explorer windows are now verified by window-handle disappearance (`NativeMethods.IsWindow`) instead of process exit, and only reported if the actual window is still open after the grace period. explorer.exe is still never force-killed.
- Follow-up 2 (same day): shipped a batch of switching-robustness features (all in one build):
  - **Single-instance guard** (root cause of "Ctrl+Space stopped working"): there was NO single-instance enforcement, so a leftover/zombie Deskify process kept the global Ctrl+Space hotkey registered and any new instance's `RegisterHotKey` silently failed (was only logged). Added a named `Mutex` (`Local\Deskify.SingleInstance.9F3A`) in `App.OnStartup`; a second launch broadcasts a registered window message (`App.ShowExistingWindowMessage`, handled in `MainWindow.WndProc`) to surface the running window, then `Shutdown()`s before creating any window. Removed `StartupUri` from App.xaml — the main window is now created manually in OnStartup only for the first instance. This also stops rebuilds from being blocked by a locked exe (only ever one instance). Hotkey-registration failure now also shows an on-screen status instead of only logging.
  - **Never-close apps list** (Settings → "Never close these apps"): `AppSettings.NeverCloseApps` (lowercase exe names). `WindowCloser.AllWindows`/`OtherWindows` take an optional never-close set and exclude those exes. UI is an ItemsControl of `NeverCloseRow` (exe name + Remove) with an "Add app…" button reusing `AppPickerWindow`; adds/removes save immediately. Purpose: protect apps the user keeps open across workspaces or that hold unsaved work (FL Studio, editors) from the clean-slate switch.
  - **Save-work warning**: `CloseOthersWindow` shows a red-bordered "⚠ Save your work first…" banner in WorkspaceSwitch mode only (a plain "Close Other Apps" doesn't clear the desktop, so no warning there).
  - **Newest-wins switching**: a new switch now cancels the in-flight one instead of being ignored by the old `_launching` guard. `MainWindow` holds a `CancellationTokenSource` + `_launchGeneration` counter; token is threaded through `LaunchEngine.LaunchAsync`/`PositionWindowsAsync` (all `Task.Delay`s + `ThrowIfCancellationRequested` at loop tops) and `WindowCloser.CloseAndVerifyAsync`. A superseded launch catches `OperationCanceledException` and skips all UI updates (generation check), so only the newest click's workspace lands on screen. Fixes browser/Explorer ending up wrong when fast-switching mid-load.
- Follow-up 3 (same day): Discord opened at its own remembered position, ignoring the saved layout. Root cause was in `LaunchEngine.PositionWindowsAsync`'s settle pass: it re-checked a **frozen HWND**. Discord (Electron) opens a transient splash/updater window first — Deskify positioned that, marked the target done, then Discord swapped in the real main window at its saved position, which was never repositioned (and the settle re-check ran against the now-dead updater HWND, a silent no-op). Also Discord re-asserts its own bounds a few seconds after load, past the old single 1s pass. Fix: the settle phase now **re-matches each positioned app against a fresh `WindowScanner.Scan()` + `ProcessTree` each pass** (via `FindBestWindow`) instead of a stored HWND, and re-applies until stable — so it re-finds the swapped-in real window and re-corrects self-restored drift. Also lengthened the settle window (non-strict 5×700ms, strict 8×700ms, breaks early once stable) since self-restoring apps take a few seconds. `applied` changed from `List<(IntPtr,WindowLayout)>` to `List<AppEntry>` to support re-matching. General fix — helps any splash-then-main or self-restoring app, not just Discord.

## 2026-07-03 (third session)
- Fixed stacked/tabbed folder saving: Windows 11 Explorer stacks folders as TABS sharing one window handle, but matching only read the first tab's path — stacked folders failed to save/restore layouts. ExplorerWindows now maps each window to ALL of its tab paths (one cached COM enumeration instead of one per window), path comparison ignores trailing backslashes, and two folder entries may share one tabbed window instead of the second reporting "not found". Same normalization applied to auto-linked entry sync and wizard folder dedupe.
- Logo unified to the outlined style (the in-app badge Will prefers): regenerated deskify.ico with stroked "blank" boxes instead of filled — exe/taskbar/shortcuts now match the badge; Light mode keeps the inverted badge by design.
- Edit Layout got its own icon (window-split IcoLayout) so it no longer shares the pencil with Edit project; the floating layout toolbar uses it too.
- Native title bar now follows the theme (DWM immersive dark mode): dark in Dark theme, light in Light, applied to main window + all dialogs and retinted live on theme switch.

## 2026-07-03 (later session)
- Autosave on exit added — nothing typed is lost when the app closes:
  - Wizard mid-edit → saved to %APPDATA%\Deskify\draft.json (project data + step + original file path); restored into the wizard at the same step on next startup, then cleared. Save/Cancel also clear it; an untouched empty new-project wizard is not drafted. Verified end-to-end (plant draft → restored+consumed; graceful close mid-wizard → draft re-written correctly).
  - Settings → valid changed values auto-apply when leaving the page or closing the app (explicit Save button kept for validation feedback). Re-clicking the Settings nav no longer wipes unsaved box edits.
  - Notes → saved on close if changed (LostFocus doesn't fire when closing with the box focused).
- New logo: monochrome rounded square with a tiled window layout (tall pane + two stacked panes), matching the app palette (#E0E0E0 bg, #121212/#8A8A8A tiles). Multi-size deskify.ico (16–256px, PNG frames) at src\Deskify.App\Assets\deskify.ico, wired via <ApplicationIcon> — exe, taskbar, window chrome, and shortcuts all use it. In-app brand badge + empty state use the matching IcoDeskify geometry in Styles.xaml. Icon generator source kept in session scratchpad (WPF render → ICO writer) — trivially recreatable.

## 2026-07-03
- Full review + stabilization pass on Deskify (0 warnings, builds clean, smoke-tested).
- Bugs fixed:
  - Wizard Cancel button was hidden under the Next button (both in the same grid column) — Cancel is now clickable.
  - Blocked/invalid URL feedback in the wizard was written to a hidden label — now shows inline next to the URL box, with basic URL validation and duplicate rejection.
  - Quick switch (Ctrl+Space): an accidental tap instantly launched the top project via release-polling; typing to filter required clicking the box while holding both keys. Reworked to a standard launcher popup: opens focused on search, type to filter, Up/Down + Enter or click to launch, Esc/click-away to close. Removed the 40 ms key-poll timer.
  - "Add Selected" with nothing selected closed the app picker as if cancelled — Add buttons in the picker and capture dialogs now stay disabled until something is selected (capture dialog also shows a live selected count).
  - Project delete could throw an unhandled exception on a locked file — now guarded, failure reported in status without dropping the project from the list.
  - Duplicate folders/URLs could be added to a project (double launch on restore) — deduped on add.
  - Edit Layout had no reentrancy guard — double-click double-launched every app; now guarded alongside Launch.
  - A matched window that Windows refused to reposition (e.g. elevated apps) was dropped silently — now reported as an error.
- Consistency/polish: scrollbar thumb color was hardcoded (broken in Light theme) — now theme-driven; "workspace"/"project" terminology unified to "project"; list rows and settings cards left-aligned to match the dialogs; checkbox template reworked so long labels wrap instead of clipping; placeholder text added to search boxes and the URL box; app(s)/window(s) strings properly pluralized; quick-switch settings copy updated; wizard progress segments rounded.

## 2026-07-01
- Initialized AI Brain structure with root instructions system and project folder organization.
- Defined core domains: applications (general), music production, and game development.
- Replaced widget-specific scope with generalized application development focus.
- Established performance-first priorities:
  - Fast startup and responsiveness
  - Low-latency UI interactions
  - Persistent session state
  - Efficient resource usage
- Set strict preference for direct, implementation-focused communication style.